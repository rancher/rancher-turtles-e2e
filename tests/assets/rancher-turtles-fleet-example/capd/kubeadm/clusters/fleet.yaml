namespace: default

helm:
  chart: ./capd-kubeadm-clusters
  values:
    clustername_prefix: turtles-qa-capd-kdm

diff:
  comparePatches:
  - apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
    kind: DockerCluster
    namespace: default
    jsonPointers:
    - "/spec/failureDomains" 
