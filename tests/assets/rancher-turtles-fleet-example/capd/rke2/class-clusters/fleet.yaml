namespace: capi-clusters

helm:
  values:
    clustername_prefix: docker-rke2-example-cluster
  valuesFrom:
    - configMapKeyRef:
        name: clusters-helm-values
        namespace: capi-clusters
        key: values.yaml