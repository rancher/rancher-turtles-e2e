namespace: default

helm:
  chart: ./capz-rke2-helmchart
  valuesFrom:
    - secretKeyRef:
        name: azure-creds-secret
        namespace: capz-system
        key: values.yaml
  values:
    clustername_prefix: turtles-qa-capz
    clusterclassname_prefix: capz-class
    namespace: capz-system
